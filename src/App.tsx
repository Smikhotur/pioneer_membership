import React, { FC } from 'react';
import { PioneerMemberships } from './components';

export const App: FC = () => {
  return (
    <div className='App'>
      <PioneerMemberships />
    </div>
  );
};
